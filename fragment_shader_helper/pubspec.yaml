name: fragment_shader_helper
description: A package to generate helper classes and fucntions from shader programs for using fragment shaders within Flutter.
version: 0.0.1
repository: https://github.com/material-components/materal-components-flutter-experimental/ # TODO: move this.

environment:
  sdk: ">=2.12.0-0 <3.0.0"

# TODO: purge deps

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  path_provider: ^2.0.0
  crypto: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.0
  mustache: ^1.1.1
  pedantic: ^1.8.0
  protobuf: ^1.0.1
